After downloading and extracting, simply run the HTML file. Program works offline, just requires a browser. The simulate button is a failsafe, your code should simulate as soon as valid G-Code is entered.
